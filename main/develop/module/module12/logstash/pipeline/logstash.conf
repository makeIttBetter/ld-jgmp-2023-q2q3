input {
  tcp {
    port => 5044
    codec => json_lines {}
  }
}

filter {
  # Add any parsing or field transformations if needed
}

output {
  stdout {
    codec => rubydebug
  }
  # Example for Elasticsearch if you want it:
  # elasticsearch {
  #   hosts => ["http://elasticsearch:9200"]
  #   index => "my-logs-%{+YYYY.MM.dd}"
  # }
}
