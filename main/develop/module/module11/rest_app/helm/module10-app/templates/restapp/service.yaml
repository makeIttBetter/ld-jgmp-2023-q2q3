apiVersion: v1
kind: Service
metadata:
  name: module11-rest-app
spec:
  selector:
    app: module11-rest-app
  ports:
    - protocol: TCP
      port: {{ .Values.restapp.port }}
      targetPort: {{ .Values.restapp.port }}
